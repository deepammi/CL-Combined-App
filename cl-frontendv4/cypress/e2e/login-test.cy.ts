describe('Login Page Test', () => { beforeEach(() => { cy.visit('/'); cy.wait(1000); }); it('should display the login page', () => { cy.url().should('include', '/'); cy.get('body').should('be.visible'); }); it('should have interactive elements', () => { cy.get('button, input, a').should('exist'); }); it('should allow basic interaction', () => { cy.get('button').first().click({ force: true }); }); });
